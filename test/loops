func myrec(n){
    if ((n == 0)){
        return nil
    }

    while ((true == true)){
        call print(n)
        break
        call print("this shouldn't be executed!")
    }

    call myrec((n - 1))
}

main {
    for (,,i = 0){
        break
    }
    call myrec(5)
}