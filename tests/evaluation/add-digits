# Leet-code problem 258

func addDigits(num){
    if ((num <= 9)){
        return num
    }

    decl sum = 0
    while ((num > 0)){
        sum = (sum + (num % 10))
        num = (num / 10)
    }

    return sum
}

main {
    call print(call addDigits(57856)) # Returns 20
}