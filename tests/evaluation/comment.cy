`
Header comment
# Single line

`

main {
    `

    comment overload

    `
   `
   Multi-line comment

   func foo(){ # This is cool
        return 22
   }
   `
                call print(2, "\n") #foo `suffix` #foo
    call print(4, "\\n")
    `Multi-line comment on single line`
    `prefix` return 22

    `return 23 # Unreachable`
}

`
This is out of bounds #
#
#
#
#
`